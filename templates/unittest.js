describe('<%=_.capitalize(file.name)%>', function () {
    describe('<%=_.capitalize(file.name)%>', function () {
        it('should do something', function () {
            // Given
            var element = new <%=_.capitalize(file.name)%>();

            //TODO: написать тест
        });
    });
});